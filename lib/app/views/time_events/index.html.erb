<h1>Listing time_events</h1>

<table>
  <tr>
  </tr>

<% @time_events.each do |time_event| %>
  <tr>
    <td><%= link_to 'Show', time_event %></td>
    <td><%= link_to 'Edit', edit_time_event_path(time_event) %></td>
    <td><%= link_to 'Destroy', time_event, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New time_event', new_time_event_path %>